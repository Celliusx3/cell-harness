"""cell-harness — an agent harness.

The package root is deliberately empty of re-exports. Import from the subpackage
that owns the type (`from harness.core.scope import Scope`), so the module graph
stays readable and a cycle is visible where it is introduced rather than hidden
behind a convenience import here.
"""
