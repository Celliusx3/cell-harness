"""Tools implemented in this process, one folder each.

`clock` and `code` live here. The `skill` tool does not: it is
built from the skills catalog and lives beside it in `harness/skills/`. Files,
search, and shell arrive with their seams later; the folder-per-tool shape is
here first so the convention does not have to change when they do.
"""
