"""Fixtures shared by every test.

Empty for now — phase 0 needs none. Integration fixtures that spawn real
processes belong in `tests/integration/conftest.py`, not here, so a unit run
never pays for them.
"""

from __future__ import annotations
